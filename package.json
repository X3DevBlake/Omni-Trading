{
  "name": "omni-trading",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:ios": "vite build && npx cap add ios && npx cap sync ios",
    "build:android": "vite build && npx cap add android && npx cap sync android",
    "build:desktop": "vite build && npx electron-packager . Omni --platform=win32,darwin"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0",
    "esbuild": "^0.28.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0",
    "@capacitor/ios": "^5.7.0",
    "electron": "^29.1.0",
    "electron-packager": "^17.1.2",
    "firebase": "^12.12.0",
    "lightweight-charts": "^5.1.0",
    "three": "^0.183.2"
  }
}
